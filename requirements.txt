# CrossCat must be installed first. Not available on pypi.
# See probcomp.csail.mit.edu/crosscat for installation instructions.
# crosscat>=0.1

# api server
jsonrpc==1.2
requests==1.0.3
Twisted==12.3.0
pyOpenSSL==0.13

# numerics and plotting
numpy==1.7.0
scipy>=0.11.0
matplotlib==1.3.1
pandas >= 0.7.1
patsy
statsmodels

# install via install_hcluster.sh if this doesn't work
hcluster==0.2.0

# for documentation generation
Sphinx==1.2b1

# bayesdb
prettytable==0.7.2

# testing
pytest>=2.5.1

# bql shell
cmd2>=0.6.7
pyparsing>=2.0.1

# starcluster, only necessary on local computer spinning up a cluster
# Jinja2==2.6
# StarCluster==0.93.3
# boto==2.3.0
# decorator==3.3.1
# pyasn1==0.0.13b
# pycrypto==2.6
# ssh==1.7.13
# workerpool==0.9.2

# for ipython notebook
ipython==0.13.2
